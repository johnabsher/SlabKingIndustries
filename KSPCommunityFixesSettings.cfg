@KSP_COMMUNITY_FIXES:AFTER[KSPCommunityFixes] {
	@CommNetThrottling = true
	@COMMNET_THROTTLING_SETTINGS
	{
		@unpackedInterval = 15 // interval when the active vessel is not timewarping
		@packedInterval = 1 // interval when the active vessel is timewarping
	}
}